apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealed-secrets
spec:
  project: system
  destination:
    namespace: sealed-secrets
    server: "https://kubernetes.default.svc"
  source:
    repoURL: "https://github.com/mhutter/rhea.git"
    targetRevision: coreos
    path: apps/sealed-secrets
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
